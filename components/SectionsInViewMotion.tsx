'use client';

import type { ReactNode } from 'react';

type SectionsInViewMotionProps = {
  children: ReactNode;
};

export default function SectionsInViewMotion({ children }: SectionsInViewMotionProps) {
  return <>{children}</>;
}
